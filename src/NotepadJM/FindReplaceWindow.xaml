<Window x:Class="NotepadJM.FindReplaceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Buscar y reemplazar" Height="245" Width="520" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="18">
        <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Buscar:" VerticalAlignment="Center" Margin="0,0,12,10"/>
        <TextBox x:Name="FindTextBox" Grid.Column="1" Margin="0,0,0,10" Padding="8"/>
        <TextBlock Grid.Row="1" Text="Reemplazar:" VerticalAlignment="Center" Margin="0,0,12,10"/>
        <TextBox x:Name="ReplaceTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10" Padding="8"/>
        <CheckBox x:Name="MatchCaseCheckBox" Grid.Row="2" Grid.Column="1" Content="Distinguir mayúsculas y minúsculas" Margin="0,0,0,14"/>
        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Buscar siguiente" Click="FindNext_Click"/>
            <Button Content="Reemplazar" Click="Replace_Click"/>
            <Button Content="Reemplazar todo" Click="ReplaceAll_Click"/>
            <Button Content="Cerrar" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
